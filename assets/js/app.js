/**
 * @class
 * @name LayoutNav
 * @module FrontEnd
 */

class LayoutNav {
  constructor()

  /**
   * @method
   * @description Check to see if a user is logged in
   * @returns {json} userProfile
   */
  checkIfLoggedIn() {
    return userProfile
  }
}
